[project]
name = "studybot-api"
version = "2.0.0"
description = "StudyBot Web API"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100
